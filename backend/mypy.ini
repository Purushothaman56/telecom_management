# mypy.ini
[mypy]
# Strictness settings (you can adjust these)
strict = True  # Enable all strictness options
# Specify files/directories to check
files = app/  # Check the 'app' directory for type consistency
# Exclude specific directories
exclude = 
    __pycache__
